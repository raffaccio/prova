<svg width="100" height="100" xmlns="http://www.w3.org/2000/svg">
  <style>
    .outer {
      fill: rgba(0, 115, 230, 0.5);
      stroke: transparent;
      stroke-width: 3;
      transition: stroke 0.4s ease, stroke-dashoffset 0.4s ease;
      stroke-dasharray: 300;
      stroke-dashoffset: 300;
    }

    .inner {
      fill: rgba(0, 115, 230, 0.3);
    }

    svg:hover .outer {
      stroke: rgba(255, 255, 255, 0.7);
      stroke-dashoffset: 0;
    }
  </style>

  <rect class="outer" x="10" y="10" width="80" height="80" rx="10" ry="10" />
  <rect class="inner" x="20" y="20" width="60" height="60" rx="15" ry="15" />
</svg>
<!-- This SVG represents a logo with an outer rectangle and an inner rectangle.
The outer rectangle has a blue color with a semi-transparent effect and a stroke that animates on hover. The inner rectangle is slightly smaller and has a lighter blue color. -->